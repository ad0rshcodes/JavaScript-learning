var text = document.getElementById("root").innerText;
console.log(text);

changeText = (message) => {
  document.getElementById("root").innerText = message;
  console.log(mess);
};

changeText("it worked!!");
